packages:
    .
    ../markup-parse
    ../circuits-parser
    ../circuits
    ../circuits-meter
    ../numhask
    ../numhask-space
    ../numhask-diff
    ../harpie
    ../harpie-numhask
    ../mealy

allow-newer:
  tdigest:base
