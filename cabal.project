packages:
    .
    ../markup-parse
    ../markup-parse-legacy
    ../circuits-parser
    ../circuits
    ../circuits-meter
    ../numhask
    ../numhask-space
    ../numhask-diff
    ../harpie
    ../harpie-numhask
    ../process-stats

allow-newer:
  tdigest:base
